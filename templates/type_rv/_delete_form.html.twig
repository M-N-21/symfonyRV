<form method="post" action="{{ path('app_type_r_v_delete', {'id': type_r_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_r_v.id) }}">
    <button class="btn">Delete</button>
</form>
