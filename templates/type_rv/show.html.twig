{% extends 'administrateur/admin.html.twig' %}

{% block title %}TypeRV{% endblock %}

{% block body %}
    <h1>TypeRV</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_r_v.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_r_v.libelle }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ type_r_v.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_r_v_index') }}">back to list</a>

    <a href="{{ path('app_type_r_v_edit', {'id': type_r_v.id}) }}">edit</a>

    {{ include('type_rv/_delete_form.html.twig') }}
{% endblock %}
